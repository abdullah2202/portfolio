name: CI Checks
on:
  pull_request:
    branches: [dev, main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Terraform
        run: |
          cd terraform/dev
          terraform init -backend=false
          terraform validate
